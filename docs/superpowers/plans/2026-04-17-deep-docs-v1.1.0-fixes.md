# Deep Docs v1.1.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `docs/ultrareview-2026-04-17.md`의 24건 결함을 v1.1.0으로 정비하되, 6 라운드 deep-review로 검증된 스펙(`docs/superpowers/specs/2026-04-17-deep-docs-ultrareview-fixes-design.md`)에 따라 `scan-filters/` 통합·artifact schema 변경·UX 개선을 runtime 문서에 반영한다.

**Architecture:** Markdown-first plugin. Python primary 알고리즘(이미 `scan-filters/` 디렉토리에 완성)을 runtime 문서(`agents/doc-scanner.md`, `commands/deep-docs.md`, `skills/.../SKILL.md`, `scan-rules.md`, `audit-metrics.md`)에서 참조하도록 통합. 8개 논리 커밋으로 쪼개져 독립 rollback 가능. 검증은 grep 기반 verify-fixes.sh + 수동 dogfood 2단계.

**Tech Stack:** Claude Code plugin (Markdown + YAML frontmatter + Bash). 알고리즘 레퍼런스는 Python 3.8+. 외부 의존성 없음.

**Scope Check:** 단일 플러그인, 단일 릴리스. 분리 불필요.

---

## File Structure

### Modify (runtime files — spec이 정의한 변경사항을 적용)

| 파일 | 책임 | 변경 커밋 |
|------|------|-----------|
| `.claude-plugin/plugin.json` | plugin manifest (버전) | Task 8 |
| `package.json` | 버전 sync + private | Task 7, 8 |
| `hooks/hooks.json` | 현재 빈 훅 파일 | **삭제** (Task 7) |
| `agents/doc-scanner.md` | scan 실행 에이전트 | Task 1, 2, 3, 4, 6 |
| `commands/deep-docs.md` | 슬래시 커맨드 | Task 1, 3, 7 |
| `skills/deep-docs-workflow/SKILL.md` | 워크플로 가이드 | Task 3 |
| `skills/deep-docs-workflow/references/scan-rules.md` | scan 규칙 참조 | Task 2, 5, 6 |
| `skills/deep-docs-workflow/references/audit-metrics.md` | audit 지표 참조 | Task 4, 5 |
| `README.md` | 영문 설명 | Task 3, 5, 7 |
| `README.ko.md` | 한국어 설명 | Task 3, 5, 7 |
| `CHANGELOG.md` | v1.1.0 섹션 | Task 8 |

### Create

| 파일 | 책임 | 커밋 |
|------|------|------|
| `.gitignore` | `.deep-docs/` 제외 | Task 7 |
| `scripts/verify-fixes.sh` | grep 기반 스펙 준수 체크 | Task 8 |

### Existing (스펙 작업으로 이미 커밋됨, 건드리지 않음)

- `skills/deep-docs-workflow/references/scan-filters/` — 6 필터 파일 + README (Python primary, Bash 참고)
- `docs/superpowers/specs/2026-04-17-deep-docs-ultrareview-fixes-design.md` — v6 최종본
- `docs/ultrareview-2026-04-17.md` — 원본 리뷰
- `.deep-review/reports/` — 리뷰 라운드 1~6 기록

### Stage (현재 untracked)

- `docs/backlog-2026-04-16.md` — 기존 untracked 파일 커밋에 포함 (Task 7)

---

## Task 1: Agent Tooling 수정 (C-2, C-3, M-4)

**Files:**
- Modify: `agents/doc-scanner.md:11-14` (tools 배열)
- Modify: `commands/deep-docs.md:2` (allowed-tools)
- Modify: `commands/deep-docs.md:36-40` (Agent pseudo-code → Task)

**대상 리뷰 결함**: C-2 (doc-scanner에 Write 없음), C-3 (allowed-tools에 `Agent`), M-4 (모델 식별자)

- [ ] **Step 1: doc-scanner.md tools에 Write 추가**

파일: `/Users/sungmin/Dev/deep-docs/agents/doc-scanner.md`

Line 10-14를 다음으로 변경:
```yaml
tools:
  - Read
  - Glob
  - Grep
  - Bash
  - Write
```

이유: Step 12(결과 저장)가 `.deep-docs/last-scan.json`을 요구하는데 Bash로 heredoc 쓰기는 취약.

- [ ] **Step 2: doc-scanner.md frontmatter 아래 model 안내 주석 추가**

파일 line 16 직후(첫 본문 heading 전)에 다음 삽입:
```markdown
<!-- Claude Code plugin은 model alias(`sonnet`) 허용. 특정 버전 고정 필요 시 `claude-sonnet-4-6` 등 full ID 사용 가능. -->
```

- [ ] **Step 3: commands/deep-docs.md allowed-tools 변경**

파일: `/Users/sungmin/Dev/deep-docs/commands/deep-docs.md` line 2

변경 전:
```
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Agent, AskUserQuestion
```

변경 후:
```
allowed-tools: Read, Write, Edit, Bash, Glob, Grep, Task, AskUserQuestion
```

- [ ] **Step 4: commands/deep-docs.md 본문 pseudo-code 교체**

파일 line 34-40 (Agent spawn 설명):
```
2. doc-scanner 에이전트를 spawn하여 스캔 실행:
   ```
   Agent(doc-scanner): "프로젝트의 에이전트 지침 문서를 스캔하세요.
   프로젝트 루트: {cwd}
   git 사용 가능: {is_git}
   scan-rules.md의 규칙을 따라 auto-fix와 audit-only를 분류하세요."
   ```
```

로 교체:
```
2. doc-scanner 에이전트를 Task 도구로 spawn:
   ```
   Task(subagent_type="doc-scanner", prompt="프로젝트의 에이전트 지침 문서를 스캔하세요.
   프로젝트 루트: {cwd}
   git 사용 가능: {is_git}
   scan-rules.md의 규칙을 따라 auto-fix와 audit-only를 분류하세요.")
   ```
```

- [ ] **Step 5: 변경 확인**

```bash
cd /Users/sungmin/Dev/deep-docs
grep -E '^\s*-\s*Write' agents/doc-scanner.md
grep '^allowed-tools:' commands/deep-docs.md | grep -c 'Task'
grep -c 'Task(subagent_type=' commands/deep-docs.md
grep -c 'Agent(doc-scanner)' commands/deep-docs.md
```

Expected:
```
  - Write
1
1 (또는 그 이상)
0 (Agent(doc-scanner) pseudo-code 잔존 없음)
```

- [ ] **Step 6: Commit**

```bash
git add agents/doc-scanner.md commands/deep-docs.md
git commit -m "fix(agent): wire up Write tool and correct Task permission

- doc-scanner tools에 Write 추가 (C-2) — last-scan.json 저장 가능
- commands allowed-tools: Agent → Task (C-3) — Claude Code 표준 도구명
- 본문 pseudo-code를 Task(subagent_type=...) 형태로 교체
- model alias 사용 안내 주석 추가 (M-4)"
```

---

## Task 2: Scan-filters 통합 (C-1, H-3, M-7, NC-1, NC-2, NEW-RCE, NEW-CLI-BYPASS, NEW-FENCE-INDENT)

**Files:**
- Modify: `skills/deep-docs-workflow/references/scan-rules.md` — 전체 재작성에 가까움
- Modify: `agents/doc-scanner.md:47-55` (참조 추출 Step), `:85-105` (중복·코드펜스·heuristic Step)

**대상 리뷰 결함**: C-1/NC-1 (번역쌍), H-3/X-3 (코드펜스 dead-ref), NC-2 (freshness), NEW-RCE (worktree-hash), NEW-CLI-BYPASS (CLI), NEW-FENCE-INDENT (들여쓰기 펜스)

**배경**: `skills/.../scan-filters/` 6개 필터 파일이 이미 존재(스펙 단계 산출물). 본 Task는 **runtime 문서(doc-scanner.md, scan-rules.md)가 이 필터들을 참조**하도록 통합.

- [ ] **Step 1: scan-rules.md 재구성 — 필터 디렉토리 참조 추가**

파일: `/Users/sungmin/Dev/deep-docs/skills/deep-docs-workflow/references/scan-rules.md`

파일 상단(제목 직후)에 다음 섹션 삽입:

```markdown
## Heuristic 세부 — `scan-filters/` 디렉토리 참조

각 규칙의 detailed algorithm은 독립 필터 파일로 분리됨 (v1.1.0 architectural split). 본 파일은 **auto-fix vs audit-only 분류 기준**만 기술하며, "어떻게 판별하는가"는 각 필터 참조.

| 규칙 | 필터 파일 |
|------|-----------|
| 번역 쌍 탐지 | `scan-filters/translation-pair.md` |
| 코드펜스 인식 + segment 분리 | `scan-filters/code-fence.md` |
| 참조 추출 | `scan-filters/reference-extraction.md` |
| CLI stale 판정 | `scan-filters/cli-whitelist.md` |
| 워크트리 해시 (artifact 재사용) | `scan-filters/worktree-hash.md` |
| Freshness 시각 비교 | `scan-filters/freshness-timestamp.md` |

**구현 언어**: Python 3.8+ primary (각 필터 파일의 알고리즘 섹션). Bash 근사는 "참고: Bash 근사 (정확성 미보장)" 섹션에만 제시됨 — 실 구현은 Python.
```

- [ ] **Step 2: scan-rules.md Rule 1(Dead References) 업데이트**

파일에서 "### 1. 죽은 참조 (Dead References)" 섹션을 찾아 **탐지 방법** 문단을 다음으로 교체:

```markdown
탐지 방법:
- **참조 추출**: `scan-filters/reference-extraction.md`의 Rule 0 순서를 따름
  - fenced block 내부 참조는 **추출 대상 아님** (코드펜스 예시 보호)
  - inline backtick 중 공백 있으면 CLI 분기, 없으면 path/env/symbol 분기
- **각 참조 검증**: `path` kind는 Glob/Grep으로 존재 확인
- **함수/클래스**: Grep으로 정의 검색
```

- [ ] **Step 3: scan-rules.md Rule 3(Stale Examples) 업데이트**

"### 3. 오래된 예시/명령어 (Stale Examples)" 섹션의 **탐지 방법** 문단 교체:

```markdown
탐지 방법:
- **CLI 명령어 stale 판정**: `scan-filters/cli-whitelist.md` 필터에 위임
  - Step 1: project lookup (npm run, make target 등)
  - Step 2: system command whitelist
  - Step 3: optional `$PATH` check (기본 OFF)
  - Step 4: 남으면 stale 후보 (UNKNOWN_SUBCOMMAND_IS_STALE=False 기본 — audit-only)
- **환경 변수 참조**: `.env.example`과 대조 (단순 존재 확인)
```

- [ ] **Step 4: scan-rules.md Rule 4(Duplicated Instructions) 업데이트**

"### 4. 중복 지침 블록 (Duplicated Instructions)" 섹션의 **탐지 방법** 문단 교체:

```markdown
탐지 방법:
- **코드펜스 분리**: `scan-filters/code-fence.md` 필터가 segment 단위로 문서 분할
  - 3 space 들여쓰기까지 fence 인식 (CommonMark 0.31)
  - tilde fence(`~~~`) 지원
- **3-line sliding window**: 각 segment **내부에서만** 해시 비교 (prose concatenation false-positive 방지)
- **번역 쌍 제외**: `scan-filters/translation-pair.md` 그룹 맵으로 동일 가족 간 중복은 auto-fix 제외
- **블록 해시**: Python `hashlib.sha1(line_triple)`로 동일성 판정 (100% 일치만 auto-fix, 유사는 audit-only)
```

- [ ] **Step 5: agents/doc-scanner.md Step 2(참조 추출) 업데이트**

파일: `/Users/sungmin/Dev/deep-docs/agents/doc-scanner.md`

line 48-55 ("### 2. 참조 추출" 섹션)을 다음으로 교체:

```markdown
### 2. 참조 추출

`scan-filters/reference-extraction.md` 필터의 Rule 0~5를 실행. 요약:

1. `code-fence.md`로 문서를 segment 배열로 분할 (fenced block 제외)
2. 각 non-fenced segment의 inline backtick 내용에 대해:
   - 첫 토큰이 CLI binary면 → `kind: "cli"` (공백 허용)
   - 공백 없는 single token이면 path/env/symbol 분기
3. Markdown link `[text](path)`의 path도 `kind: "path"`로 추출
4. Indented code block(4+ space)은 제외

**중요**: fenced code block 내부의 `import` 문·예시 경로는 **추출 대상 아님**. 코드블록은 의도된 예시이므로 dead-reference 판정 안 함.
```

- [ ] **Step 6: agents/doc-scanner.md Step 6(중복 탐지) 업데이트**

line 87-91 ("### 6. 중복 탐지" 섹션) 교체:

```markdown
### 6. 중복 탐지

`scan-filters/code-fence.md`와 `scan-filters/translation-pair.md` 필터 조합:

1. `code-fence.md`로 각 문서를 non-fenced segment 리스트로 분할
2. 3-line sliding window 해시를 **각 segment 내부에서만** 계산 (segment 경계 교차 매칭 금지 — prose concatenation false-positive 방지)
3. cross-document 3-line 일치 발견 시, `translation-pair.md`의 그룹 맵 조회:
   - 양 문서가 동일 그룹 → **audit-only** (번역 쌍의 의도된 동일 내용)
   - 다른 그룹 또는 그룹 외 → **auto-fix** (중복 제거 제안)

**그룹 키 계산**: 디렉토리 경로 포함. `docs/api/README.md`와 `docs/setup/README.ko.md`는 **다른 그룹** (같은 basename이지만 다른 dir).
```

- [ ] **Step 7: agents/doc-scanner.md Step 7(크기 검사) 업데이트 — strict > 부등호**

line 93-98 ("### 7. 크기 검사" 섹션) 교체:

```markdown
### 7. 크기 검사 (Size Check)

각 문서의 라인 수를 측정 (strict `>` 부등호 — 경계값에서 경고+만점 충돌 방지, 리뷰 CX-2 대응):
- CLAUDE.md, AGENTS.md: `>100`이면 경고 (분리 제안)
- README.md: `>300`이면 경고
- 기타 docs/: `>200`이면 경고

분류: auto-fix (제안만, 자동 분리 안 함)
```

- [ ] **Step 8: 변경 확인**

```bash
cd /Users/sungmin/Dev/deep-docs
grep -c 'scan-filters/' skills/deep-docs-workflow/references/scan-rules.md
grep -c 'scan-filters/' agents/doc-scanner.md
grep -c '**audit-only**' skills/deep-docs-workflow/references/scan-rules.md
```

Expected:
```
>= 6 (6개 필터 각각 언급)
>= 2 (Step 2, Step 6에서 최소 2번)
>= 1
```

- [ ] **Step 9: Commit**

```bash
git add agents/doc-scanner.md skills/deep-docs-workflow/references/scan-rules.md
git commit -m "feat(scan): integrate scan-filters pipeline into doc-scanner and rules

- scan-rules.md 상단에 필터 디렉토리 포인터 테이블 추가
- Rule 1/3/4의 탐지 방법을 각 필터 파일 참조로 위임
- doc-scanner Step 2/6/7을 scan-filters 기반으로 재작성
- Step 7 size check strict > 부등호 (CX-2)

해결: C-1, H-3, M-7, NC-1, NC-2, NEW-RCE, NEW-CLI-BYPASS, NEW-FENCE-INDENT"
```

---

## Task 3: Artifact Provenance 확장 (H-1, H-2, H-5)

**Files:**
- Modify: `agents/doc-scanner.md:125-170` (Step 12 "결과 저장" + 재사용 규칙)
- Modify: `skills/deep-docs-workflow/SKILL.md:30-34` (재사용 조건)
- Modify: `commands/deep-docs.md:69-74` (garden TTL 설명)
- Modify: `README.md:142-146`, `README.ko.md:142-146` (설정 섹션)

**대상 리뷰 결함**: H-1 (worktree 변경 감지), H-2 (garden→invalidate), H-5 (non-git provenance)

- [ ] **Step 1: agents/doc-scanner.md Step 12 JSON 예시 교체**

파일 line 128-165 ("### 12. 결과 저장" 섹션)의 JSON 블록을 다음으로 교체:

```json
{
  "scanned_at": "2026-04-17T10:00:00Z",
  "schema_version": 2,
  "provenance": {
    "is_git": true,
    "head_sha": "abc123",
    "branch": "main",
    "worktree_hash": "3f8a..."
  },
  "documents": [
    {
      "path": "CLAUDE.md",
      "issues": [
        {
          "type": "dead-reference",
          "category": "auto-fix",
          "severity": "high",
          "line": 42,
          "current_value": "src/auth/middleware.ts",
          "suggested_value": "src/auth/auth-middleware.ts",
          "evidence": "git rename detected"
        }
      ],
      "metrics": {
        "size_lines": 85,
        "freshness_score": 7,
        "reference_accuracy": 0.85,
        "duplication_count": 1
      }
    }
  ],
  "summary": {
    "total_issues": 5,
    "auto_fixable": 3,
    "audit_only": 2
  }
}
```

변경점:
- `schema_version: 2` (breaking change 공식화)
- `provenance.worktree_hash` 필드 추가
- `issues[].reference` → `current_value` / `suggestion` → `suggested_value` (M-6)
- `freshness_score: 6` → `7` (NC-2 스케일 정규화)

- [ ] **Step 2: agents/doc-scanner.md 재사용 규칙 업데이트**

파일 line 167-170을 교체:

```markdown
garden/audit 실행 시 `.deep-docs/last-scan.json` 확인 (재사용 4-요소 규칙):

1. `schema_version == 2` — 버전 불일치 시 재-scan
2. `scanned_at`이 현재 기준 10분 이내
3. `provenance.head_sha == git rev-parse HEAD` (git 환경만)
4. `provenance.worktree_hash == scan-filters/worktree-hash.md 재계산값` (git 환경만)

하나라도 불일치하면 재-scan. **garden이 1건이라도 수정 적용 시** 종료 시 아티팩트 삭제 → 다음 audit은 반드시 재-scan.

**non-git 환경**: `provenance = { "is_git": false }` 만. 재사용은 `scanned_at` 10분 TTL만 판단.
```

- [ ] **Step 3: skills/deep-docs-workflow/SKILL.md 업데이트**

파일: `/Users/sungmin/Dev/deep-docs/skills/deep-docs-workflow/SKILL.md`

line 30-34 영역(garden 워크플로우의 "1. `.deep-docs/last-scan.json` 확인")을 다음으로 교체:

```markdown
1. `.deep-docs/last-scan.json` 확인 (재사용 4-요소):
   - `schema_version == 2` (불일치 시 재-scan)
   - `scanned_at` 10분 이내
   - `provenance.head_sha` 일치 (git 환경)
   - `provenance.worktree_hash` 일치 (git 환경, `scan-filters/worktree-hash.md` 재계산)
   - 하나라도 실패 → 재-scan
   - non-git: scanned_at 10분 TTL만
```

audit 워크플로우 섹션(45-48줄 근처)도 동일하게 업데이트.

- [ ] **Step 4: commands/deep-docs.md garden step 1 업데이트**

파일 line 69-74를 교체:

```markdown
1. `.deep-docs/last-scan.json` 확인 (재사용 4-요소):
   - `schema_version == 2`
   - `scanned_at` 10분 이내
   - `provenance.head_sha` 일치 (git)
   - `provenance.worktree_hash` 일치 (git, `scan-filters/worktree-hash.md` 재계산)
   - 하나라도 실패 → 재-scan
   - non-git 환경: scanned_at TTL만
```

audit step 1 (line 115-118 근처)도 동일 업데이트.

- [ ] **Step 5: commands/deep-docs.md garden 종료 시 invalidation 추가**

파일 line 107 직후(garden step 5 "수정 요약" 뒤)에 새 step 6 추가:

```markdown
6. **아티팩트 무효화** (리뷰 H-2 대응):
   - 1건 이상 수정 적용 시 `.deep-docs/last-scan.json` 삭제
   - 다음 audit/garden은 반드시 재-scan
```

- [ ] **Step 6: README.md 설정 섹션 업데이트**

파일: `/Users/sungmin/Dev/deep-docs/README.md`

line 114-140의 JSON 예시 블록에서 `schema_version: 2` 반영, `provenance`에 `worktree_hash` 필드 추가, `metrics.freshness_score`를 `7`로 변경, issue 객체 필드명을 `current_value`/`suggested_value`로 변경.

라인 142-146의 재사용 조건 설명을 **4-요소**로 업데이트:
```markdown
`garden` and `audit` reuse this artifact if ALL hold:
- `schema_version == 2`
- Created within **10 minutes**
- `provenance.head_sha` matches `git rev-parse HEAD` (git env)
- `provenance.worktree_hash` matches recomputation (git env)

The `worktree_hash` covers tracked diff + untracked file list/content (NUL-safe, per-file git-hash-object). See `scan-filters/worktree-hash.md`.

In non-git environments, only the 10-minute TTL applies.
```

- [ ] **Step 7: README.ko.md 동일 업데이트**

파일 `/Users/sungmin/Dev/deep-docs/README.ko.md` line 114-146을 README.md와 동일하게 번역 업데이트.

- [ ] **Step 8: 변경 확인**

```bash
cd /Users/sungmin/Dev/deep-docs
grep -c 'worktree_hash' agents/doc-scanner.md commands/deep-docs.md skills/deep-docs-workflow/SKILL.md README.md README.ko.md
grep -c 'schema_version.*2' agents/doc-scanner.md README.md README.ko.md
grep -c '4-요소\|four-factor\|4-factor' commands/deep-docs.md skills/deep-docs-workflow/SKILL.md
! grep -q '"freshness_score": 6' agents/doc-scanner.md
```

Expected: 각 파일 수치 1+ 또는 0(음성 체크).

- [ ] **Step 9: Commit**

```bash
git add agents/doc-scanner.md commands/deep-docs.md skills/deep-docs-workflow/SKILL.md README.md README.ko.md
git commit -m "feat(artifact): add worktree_hash provenance and garden invalidation

- schema_version 1 → 2 (breaking: field rename, H-1)
- provenance에 worktree_hash 추가 (scan-filters/worktree-hash.md 계산, X-2)
- 재사용 조건 3 → 4 요소 (schema_version 체크 포함, O-4)
- garden 수정 시 아티팩트 삭제 (H-2)
- non-git 환경 provenance 필드 최소화 (H-5)"
```

---

## Task 4: Freshness 스케일 & Stale 기준 (H-4, H-6, M-1)

**Files:**
- Modify: `skills/deep-docs-workflow/references/audit-metrics.md` (Freshness 섹션)
- Modify: `agents/doc-scanner.md:74-84` (Step 5)

**대상 리뷰 결함**: H-4 (스케일), H-6 (mtime fallback), M-1 (대부분/일부 기준)

- [ ] **Step 1: audit-metrics.md Freshness 섹션 재작성**

파일 line 14-26 ("### 2. 신선도 (Freshness)")을 교체:

```markdown
### 2. 신선도 (Freshness) — Path-scoped

측정 방법 (`scan-filters/freshness-timestamp.md` 위임):
1. 문서가 참조하는 파일 경로 추출
2. 각 경로의 `last_modified_epoch`:
   - git 커밋 시각: `git log -1 --format=%ct -- <path>` (epoch)
   - 파일시스템 mtime: `stat -f %m` (BSD) / `stat -c %Y` (GNU)
   - **dirty 파일만** `max(fs_ts, git_ts)` 채택 (clean checkout에서 mtime이 git time보다 커도 git time 사용)
3. 문서 자체의 `last_modified_epoch`도 동일 방식
4. 참조 경로의 last_modified > 문서의 last_modified → stale

**점수 스케일** (허용 값 `{10, 7, 4, null}`):

| stale 비율 | 점수 |
|------------|------|
| `<30%` | 10 |
| `30–70%` | 7 |
| `≥70%` | 4 |
| 참조 경로 없음 | `null` (평균에서 제외) |

**경계 명확화** (M-1): 재현성 위해 정량 구간 정의. 비율은 `stale_count / total_refs`이며 `total_refs`에서 **존재하지 않는 경로는 제외**(BU-5).
```

- [ ] **Step 2: agents/doc-scanner.md Step 5(신선도 평가) 업데이트**

파일 line 74-84를 교체:

```markdown
### 5. 신선도 평가 (path-scoped)

`scan-filters/freshness-timestamp.md` 필터 사용. 요약:

1. `get_dirty_files()` 세션 시작 시 한 번 계산 (`git diff HEAD --name-only` + `git ls-files --others --exclude-standard`)
2. 각 문서의 참조 경로에 대해:
   - `last_modified_epoch(path, dirty_files)` 호출
   - dirty 파일만 mtime 고려, clean 파일은 git commit time
   - 존재하지 않는 파일은 `None` 반환 → freshness 계산에서 제외 (total_refs에 카운트 안 됨)
3. 비율 계산:
   - `stale_ratio = stale_count / valid_total_refs`
   - `<0.30` → freshness_score = 10
   - `0.30 ≤ ratio < 0.70` → 7
   - `≥0.70` → 4
   - `valid_total_refs == 0` → `null`

결과 예시 JSON: `"freshness_score": 7` (6 같은 스케일 외 값 사용 금지).
```

- [ ] **Step 3: 변경 확인**

```bash
cd /Users/sungmin/Dev/deep-docs
grep -c 'dirty_files\|dirty 파일' agents/doc-scanner.md skills/deep-docs-workflow/references/audit-metrics.md
grep -E '30%|70%' skills/deep-docs-workflow/references/audit-metrics.md | wc -l
! grep -q '"freshness_score": 6' agents/doc-scanner.md
```

Expected: 각 파일 2+ 매치, 마지막 체크 성공.

- [ ] **Step 4: Commit**

```bash
git add agents/doc-scanner.md skills/deep-docs-workflow/references/audit-metrics.md
git commit -m "fix(audit): unify freshness scale and define stale thresholds

- 스케일 {10,7,4,null}로 정규화 (H-4)
- <30% / 30-70% / ≥70% 구간 명확화 (M-1)
- dirty-only mtime fallback (H-6, BU-6)
- 존재하지 않는 경로는 total_refs 제외 (BU-5)
- 예시 JSON의 freshness_score: 6 → 7"
```

---

## Task 5: Size 임계값 & 밴드 규칙 (M-2, M-3, L-6)

**Files:**
- Modify: `skills/deep-docs-workflow/references/audit-metrics.md` (Size, 밴드, §5)
- Modify: `skills/deep-docs-workflow/references/scan-rules.md` (Rule 5)
- Modify: `README.md`, `README.ko.md` (audit 점수 밴드 표)

**대상 리뷰 결함**: M-2 (반올림·밴드), M-3 (크기 임계값), L-6 (맵 vs 매뉴얼)

- [ ] **Step 1: audit-metrics.md Size 테이블 업데이트**

파일 line 5-12를 교체:

```markdown
### 1. 파일 크기 (Size)

strict `>` 부등호로 scan 경고와 audit 만점 경계 일치 (CX-2 대응):

| 파일 | audit 점수 | scan 경고 |
|------|------------|-----------|
| CLAUDE.md / AGENTS.md | `≤100: 10`, `100 < x ≤ 200: 7`, `>200: 4` | `>100` |
| README.md | `≤300: 10`, `300 < x ≤ 500: 7`, `>500: 4` | `>300` |
| 기타 docs/ | `≤200: 10`, `200 < x ≤ 400: 7`, `>400: 4` | `>200` |

**라인 카운트 규약**: `wc -l` 기준 (trailing newline 없으면 +1 보정 필요). 경계값 판정은 Python `len(lines)`로 확인 가능.
```

- [ ] **Step 2: audit-metrics.md 종합 점수/밴드 업데이트**

파일 마지막 부분("## 종합 점수" 섹션)을 교체:

```markdown
## 종합 점수

측정 가능한 지표 평균, **소수점 1자리** 반올림 (`Math.round(score * 10) / 10`):

| 점수 구간 | 밴드 |
|-----------|------|
| `score ≥ 9.0` | 🟢 Excellent |
| `7.0 ≤ score < 9.0` | 🟡 Good |
| `5.0 ≤ score < 7.0` | 🟠 Fair |
| `score < 5.0` | 🔴 Poor |

문서에 참조 경로가 없어 특정 지표를 측정 못하면 해당 지표 제외하고 나머지로 평균.
```

- [ ] **Step 3: audit-metrics.md §5 Map vs Manual 공식화**

파일 line 47-53을 교체:

```markdown
### 5. 맵 vs 매뉴얼 비율 (audit-only)

**외부 포인터 라인** — 다음 중 하나:
- `[텍스트](URL)` 형태 또는 `[텍스트](상대경로)`
- `"참고:"`, `"see also"`, `"자세한 내용은"`, `"refer to"` 같은 리드 포함
- 단독 URL 라인 (`^https?://`)

**직접 지침 라인** — 외부 포인터도, 빈 라인도, 헤딩(`^#`)도, 코드펜스 라인도 아닌 평문 라인

**비율** = `외부 포인터 라인 수 / (외부 포인터 + 직접 지침 라인 수)`

표시만 하고 점수 매기지 않음 (프로젝트마다 최적 비율 다름).
```

- [ ] **Step 4: scan-rules.md Rule 5 (Size) 업데이트**

파일에서 "### 5. 크기/구성 (Size/Organization)" 섹션 **탐지** 문구를 다음으로 교체:

```markdown
탐지: strict `>` 부등호 — `CLAUDE.md/AGENTS.md > 100`, `README.md > 300`, `기타 docs/ > 200` (audit-metrics.md §1과 경계 일치)
```

- [ ] **Step 5: README.md 밴드 표 업데이트**

파일 line 95-102 ("**Scoring bands:**" 테이블)을 교체:

```markdown
**Scoring bands:**

| Score | Band |
|-------|------|
| `≥ 9.0` | Excellent |
| `7.0 ≤ score < 9.0` | Good |
| `5.0 ≤ score < 7.0` | Fair (gardening recommended) |
| `< 5.0` | Poor (immediate attention needed) |

The overall score is rounded to 1 decimal (e.g., `8.5`). If a doc has no outbound references (freshness cannot be measured), that dimension is excluded and the remaining dimensions are averaged instead.
```

- [ ] **Step 6: README.ko.md 밴드 표 동일 업데이트 (한국어)**

line 95-102 ("**점수 등급:**" 테이블)을 교체:

```markdown
**점수 등급:**

| 점수 | 등급 |
|------|------|
| `≥ 9.0` | Excellent (우수) |
| `7.0 ≤ score < 9.0` | Good (양호) |
| `5.0 ≤ score < 7.0` | Fair (정비 권장) |
| `< 5.0` | Poor (즉시 정비 필요) |

종합 점수는 소수점 1자리로 반올림됩니다 (예: `8.5`). 문서에 외부 참조가 없어 특정 지표를 측정할 수 없는 경우 해당 지표를 제외하고 나머지로 평균을 산출합니다.
```

- [ ] **Step 7: 변경 확인**

```bash
cd /Users/sungmin/Dev/deep-docs
grep -c 'score ≥ 9.0\|score.*≥.*9\.0\|≥ 9.0\|`≥ 9.0`' audit-metrics.md README.md README.ko.md skills/deep-docs-workflow/references/audit-metrics.md
grep -c 'strict.*>\|>100\|>300\|>200' skills/deep-docs-workflow/references/audit-metrics.md skills/deep-docs-workflow/references/scan-rules.md
```

Expected: 각 파일에 해당 패턴 존재.

- [ ] **Step 8: Commit**

```bash
git add skills/deep-docs-workflow/references/audit-metrics.md skills/deep-docs-workflow/references/scan-rules.md README.md README.ko.md
git commit -m "fix(audit): unify size thresholds and add rounding/band rules

- size scan 경고와 audit 만점 경계 정렬 (strict >, CX-2)
- 종합 점수 소수점 1자리 반올림 (M-2)
- 밴드를 부등호로 명확화: ≥9.0 Excellent, 7.0≤<9.0 Good 등
- §5 Map vs Manual 계산 공식화 (L-6)
- README EN/KO 모두 반영"
```

---

## Task 6: Rule/Step 번호 정렬 + issue 필드명 (M-5, M-6)

**Files:**
- Modify: `skills/deep-docs-workflow/references/scan-rules.md` (Rule 번호)
- Modify: `agents/doc-scanner.md` (Step 번호 + issue 필드)

**대상 리뷰 결함**: M-5 (번호 불일치), M-6 (issue 필드명 모호)

- [ ] **Step 1: scan-rules.md 규칙 번호 매핑 표 추가**

파일 상단에 섹션 추가 (Heuristic 참조 테이블 뒤):

```markdown
## 규칙 번호 ↔ doc-scanner Step 매핑

| 규칙 | doc-scanner.md Step |
|------|---------------------|
| Rule 1 (Dead References) | Step 2 (참조 추출) + Step 3 (참조 검증) |
| Rule 2 (Moved/Renamed Paths) | Step 4 (이동 추적) |
| Rule 3 (Stale Examples) | Step 3 (CLI 판정 — `scan-filters/cli-whitelist.md`) |
| Rule 4 (Duplicated Instructions) | Step 6 (중복 탐지) |
| Rule 5 (Size/Organization) | Step 7 (크기 검사) |
| Rule 6 (Rule-Code Contradiction) — audit-only | Step 8 |
| Rule 7 (Coverage Gap) — audit-only | Step 9 |
| Rule 8 (Map vs Manual) — audit-only | Step 10 |

구현자 참고: Rule은 분류 기준, Step은 실행 순서. 두 번호 체계는 동일 작업을 다른 관점에서 참조.
```

- [ ] **Step 2: agents/doc-scanner.md issue JSON 필드명 주석 추가**

파일 Step 12 JSON 블록 직후(약 line 166)에 필드 설명 추가:

```markdown
**issue 객체 필드** (schema_version: 2):

- `type`: 허용 enum `dead-reference | moved-path | stale-example | duplicate-block | size-warning`
- `category`: `"auto-fix"` 또는 `"audit-only"`
- `severity`: `"high"` | `"medium"` | `"low"` (garden prompt 순서 결정)
- `line`: 이슈 발생 라인 번호
- `current_value`: 문서에 현재 기록된 값 (예: `"src/auth/middleware.ts"`)
- `suggested_value`: 수정 제안 값 (예: `"src/auth/auth-middleware.ts"`)
- `evidence`: 판단 근거 문자열 (예: `"git rename detected"`, `"not in package.json scripts"`)

**사용자 표시 레이블 매핑** (garden prompt에 사용):

| `type` | 한국어 레이블 |
|--------|--------------|
| `dead-reference` | 죽은 참조 |
| `moved-path` | 이동/리네임된 경로 |
| `stale-example` | 오래된 예시/명령어 |
| `duplicate-block` | 중복 지침 블록 |
| `size-warning` | 크기 초과 |
```

- [ ] **Step 3: agents/doc-scanner.md Step 번호 주석 업데이트**

각 Step heading 뒤에 "Rule N ↔ Step M" 주석을 짧게 추가. 예:

```markdown
### 2. 참조 추출

<!-- scan-rules.md Rule 1(Dead References)의 "탐지" 단계. -->
...

### 3. 참조 검증

<!-- scan-rules.md Rule 1 + Rule 3의 "검증" 단계. CLI 참조는 scan-filters/cli-whitelist.md로 위임. -->
...
```

- [ ] **Step 4: 변경 확인**

```bash
cd /Users/sungmin/Dev/deep-docs
grep -c 'Rule.*Step\|규칙.*Step' skills/deep-docs-workflow/references/scan-rules.md
grep -c 'current_value\|suggested_value' agents/doc-scanner.md
grep -c 'dead-reference.*죽은 참조\|한국어 레이블' agents/doc-scanner.md
```

Expected: 각 1+.

- [ ] **Step 5: Commit**

```bash
git add agents/doc-scanner.md skills/deep-docs-workflow/references/scan-rules.md
git commit -m "refactor(scan): align rule/step numbering and clarify issue field names

- scan-rules.md에 Rule → doc-scanner Step 매핑 표 (M-5)
- issue 객체 필드 설명 추가: current_value/suggested_value (M-6)
- type enum 허용값 + 한국어 레이블 매핑 테이블 (O-3)
- 각 Step에 Rule 참조 주석"
```

---

## Task 7: Garden UX + Housekeeping (M-8, M-9, L-1~L-5)

**Files:**
- Modify: `commands/deep-docs.md` (garden 배치 승인 + new artifact)
- Modify: `package.json` (private: true)
- Delete: `hooks/hooks.json`
- Create: `.gitignore`
- Stage: `docs/backlog-2026-04-16.md` (기존 untracked)
- Modify: `README.md`, `README.ko.md` (설치 섹션)

**대상 리뷰 결함**: M-8 (ignored 이력), M-9 (batch 승인), L-1 (backlog 커밋), L-2 (package.json private), L-3 (hooks.json 삭제), L-4 (설치 안내), L-5 (.gitignore)

- [ ] **Step 1: commands/deep-docs.md garden step 3 업데이트 (5지선다)**

파일 line 80-93 (garden step 3 "각 항목을 순서대로 처리") 교체:

```markdown
3. 각 항목을 순서대로 처리:

   a. 수정 내용을 diff로 보여줌:
   ```
   ## 수정 1/N: {파일} — {한국어 type 레이블}
   
   - `{current_value}` → `{suggested_value}` ({evidence})
   ```
   
   b. AskUserQuestion으로 5지선다:
   - **(A) 적용**
   - **(B) 건너뜀 (이번만)**
   - **(C) 건너뜀 + 기록** (`.deep-docs/garden-ignored.json`에 signature 저장)
   - **(D) 이하 모두 적용 — "{한국어 레이블}" 일괄 수락** (현재 세션 + 동일 type)
   - **(E) 이하 모두 건너뜀 — "{한국어 레이블}" 일괄 거부** (현재 세션 + 동일 type)
   
   c. A/D 선택 → Edit tool로 수정 적용
      C 선택 → `.deep-docs/garden-ignored.json` append + skip
      B/E 선택 → skip
   
   **세션 정의**: 단일 `/deep-docs garden` 호출 (시작 ~ 종료) 내에서만 (D)/(E) 선택 유지. 호출 종료 시 in-memory state 소실 (단, C만 영구 기록).
   
   **플랫폼 제약 (리뷰 O-7)**: `AskUserQuestion`이 5 옵션 미지원이면 fallback — (A)/(B)/(C)/(Batch) 4지선다로 축소 후 Batch 선택 시 별도 질문으로 (D)/(E) 구분.
```

- [ ] **Step 2: commands/deep-docs.md garden-ignored.json 섹션 추가**

파일 맨 아래 (garden 섹션 끝, audit 섹션 시작 전) 새 서브섹션 추가:

```markdown
### .deep-docs/garden-ignored.json 스키마 (M-8)

```json
{
  "schema_version": 1,
  "ignored": [
    {
      "signature": "sha256:...",
      "type": "dead-reference",
      "path": "CLAUDE.md",
      "content_preview": "src/auth/middleware.ts",
      "ignored_at": "2026-04-17T10:05:00Z"
    }
  ]
}
```

- **signature**: `sha256(type + "|" + path + "|" + content_preview[:200])`
- garden 실행 시 각 auto-fix 항목의 signature 계산 → `ignored`에 있으면 prompt skip
- 사용자가 재검토 원하면 `.deep-docs/garden-ignored.json` 수동 삭제
```

- [ ] **Step 3: package.json에 private: true 추가 + 버전 유지**

파일: `/Users/sungmin/Dev/deep-docs/package.json`

현재:
```json
{
  "name": "deep-docs",
  "version": "1.0.0",
  ...
}
```

`"name"` 키 뒤에 `"private": true,` 추가:

```json
{
  "name": "deep-docs",
  "private": true,
  "version": "1.0.0",
  ...
}
```

(버전 bump는 Task 8에서 수행)

- [ ] **Step 4: hooks/hooks.json 삭제**

```bash
cd /Users/sungmin/Dev/deep-docs
rm hooks/hooks.json
```

디렉토리가 비면 디렉토리도 제거:
```bash
rmdir hooks 2>/dev/null || true
```

- [ ] **Step 5: .gitignore 생성**

파일: `/Users/sungmin/Dev/deep-docs/.gitignore`

```gitignore
# Deep Docs scan artifacts (regenerated per scan)
.deep-docs/

# Editor/OS artifacts
.DS_Store
*.swp
*~
.vscode/
.idea/
```

- [ ] **Step 6: README.md 설치 섹션 보강**

파일 line 148-152 ("## Installation" 섹션) 교체:

```markdown
## Installation

### From Claude Code Marketplace

```bash
claude plugin add deep-docs
```

### From Git URL (development / pre-release)

```bash
claude plugin add https://github.com/Sungmin-Cho/claude-deep-docs.git
```

After install, run `/deep-docs` in any project directory. The plugin auto-creates `.deep-docs/` on first use.
```

- [ ] **Step 7: README.ko.md 설치 섹션 보강 (한국어)**

파일 line 148-152 교체 — EN과 동일하되 한국어:

```markdown
## 설치

### Claude Code Marketplace에서

```bash
claude plugin add deep-docs
```

### Git URL에서 (개발 / 사전 공개)

```bash
claude plugin add https://github.com/Sungmin-Cho/claude-deep-docs.git
```

설치 후 프로젝트 디렉토리에서 `/deep-docs`를 실행하세요. 최초 사용 시 `.deep-docs/`가 자동 생성됩니다.
```

- [ ] **Step 8: 변경 확인**

```bash
cd /Users/sungmin/Dev/deep-docs
grep -c '"private": true' package.json
[ ! -f hooks/hooks.json ] && echo "hooks.json removed"
grep -q '^\.deep-docs/' .gitignore && echo ".gitignore OK"
grep -c 'garden-ignored.json' commands/deep-docs.md
grep -c '5지선다\|(A) 적용\|(E) 이하 모두 건너뜀' commands/deep-docs.md
```

Expected:
```
1
hooks.json removed
.gitignore OK
>= 1
>= 2
```

- [ ] **Step 9: Commit**

```bash
git add commands/deep-docs.md package.json .gitignore docs/backlog-2026-04-16.md README.md README.ko.md
git rm hooks/hooks.json
git commit -m "feat(garden): batch approval + ignored history, plus housekeeping

Garden UX:
- 5지선다 prompt (M-9): A/B/C/D/E — apply/skip/skip+record/batch-apply/batch-skip
- .deep-docs/garden-ignored.json 스키마 (M-8) — signature 기반 skip 이력
- 세션 정의 명확화 (O-3)
- AskUserQuestion 플랫폼 제약 fallback (O-7)

Housekeeping:
- docs/backlog-2026-04-16.md 커밋 포함 (L-1)
- package.json에 private: true (L-2)
- hooks/hooks.json 삭제 (L-3) — v1.1 시점 active hook 없음
- README 설치 섹션에 git URL 옵션 추가 (L-4)
- .gitignore 생성 — .deep-docs/ 제외 (L-5)"
```

---

## Task 8: Release Prep — v1.1.0, CHANGELOG, verify-fixes.sh

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `package.json`
- Modify: `CHANGELOG.md`
- Create: `scripts/verify-fixes.sh`

- [ ] **Step 1: plugin.json 버전 업데이트**

파일: `/Users/sungmin/Dev/deep-docs/.claude-plugin/plugin.json`

`"version": "1.0.0"` → `"version": "1.1.0"`:

```json
{
  "name": "deep-docs",
  "version": "1.1.0",
  ...
}
```

- [ ] **Step 2: package.json 버전 업데이트**

파일: `/Users/sungmin/Dev/deep-docs/package.json`

`"version": "1.0.0"` → `"version": "1.1.0"`:

```json
{
  "name": "deep-docs",
  "private": true,
  "version": "1.1.0",
  ...
}
```

- [ ] **Step 3: CHANGELOG.md v1.1.0 섹션 추가**

파일: `/Users/sungmin/Dev/deep-docs/CHANGELOG.md`

파일 상단(`# Changelog` 직후, `## [1.0.0]` 앞)에 다음 삽입:

```markdown
## [1.1.0] - 2026-04-17

### Breaking Changes
- **`.deep-docs/last-scan.json` `schema_version` 1 → 2**. issue 필드 rename: `reference` → `current_value`, `suggestion` → `suggested_value`. v1.0 아티팩트는 자동 재생성(10분 TTL + schema 체크).
- Audit 점수 표기 변경: 정수 구간(`9–10, 7–8, 5–6, 1–4`) → 소수점 1자리 + strict 부등호(`≥9.0, 7.0≤<9.0, 5.0≤<7.0, <5.0`).

### Added
- `skills/deep-docs-workflow/references/scan-filters/` 디렉토리 — Python primary heuristic 필터 6개:
  - `translation-pair.md`: 번역 쌍 그룹핑 (ISO 639-1 allowlist + 디렉토리 경로 포함 그룹 키 + script subtag 지원)
  - `code-fence.md`: CommonMark 0.31 fence 인식 (3 space 들여쓰기까지) + per-segment hashing
  - `reference-extraction.md`: Rule 0 분기(CLI → path → env → symbol) — fenced block 내부 제외
  - `cli-whitelist.md`: 2단계 lookup (project scripts → system whitelist) + 각 매니저 built-in 집합
  - `worktree-hash.md`: NUL-safe + per-file `git hash-object` + streaming 64KB chunk
  - `freshness-timestamp.md`: epoch 비교 + dirty-only mtime fallback
- `.deep-docs/garden-ignored.json` 아티팩트 — garden 거부 항목 signature 기반 영구 skip
- Garden 5지선다 prompt — batch approval/rejection
- `scripts/verify-fixes.sh` — grep 기반 spec 준수 체크
- `.gitignore` — `.deep-docs/` 제외

### Changed
- `doc-scanner` agent tools에 `Write` 추가 — artifact 저장 가능
- `commands/deep-docs.md` allowed-tools: `Agent` → `Task` (Claude Code 표준)
- Artifact 재사용 조건 3-요소 → 4-요소 (`schema_version` 추가)
- Size 임계값 strict `>` 부등호로 scan 경고와 audit 만점 경계 정렬
- Freshness 점수 스케일 `{10, 7, 4, null}` 정규화, stale 비율 구간 `<30% / 30-70% / ≥70%` 명시
- issue `type` enum 허용값 확정 + 한국어 레이블 매핑

### Removed
- `hooks/hooks.json` — v1.1 시점 active hook 없음

### Fixed
- **Self-corruption bug (C-1)**: `README.md` ↔ `README.ko.md` 번역 쌍의 JSON 예시가 중복으로 오판되어 garden이 삭제 제안하던 이슈
- **CLI false-positive (M-7)**: `git log -1`, `find`, `wc` 등 시스템 명령이 stale로 오판
- **워크트리 변경 미감지 (H-1)**: HEAD SHA + TTL만으로는 uncommitted 변경 감지 못함 — `worktree_hash`로 해결
- **Garden 후 stale audit (H-2)**: 수정 적용 후 재사용 아티팩트로 점수 계산하던 이슈
- **macOS 비호환**: `sha1sum` → `shasum -a 1`, `stat -c` / `stat -f` fallback chain

### Security
- `worktree_hash` 계산에서 `xargs -I{} sh -c` 제거 — 악성 파일명(`$(…)`, backtick 포함) RCE 차단
```

- [ ] **Step 4: scripts/verify-fixes.sh 생성**

파일: `/Users/sungmin/Dev/deep-docs/scripts/verify-fixes.sh`

```bash
#!/usr/bin/env bash
# verify-fixes.sh — grep 기반 스펙 준수 체크
# 사용: bash scripts/verify-fixes.sh
# 목적: v1.1.0 수정사항이 runtime 문서에 반영됐는지 빠르게 확인

set -u
cd "$(dirname "$0")/.."
fail=0
pass=0

check() {
  local name="$1" cmd="$2"
  if eval "$cmd" >/dev/null 2>&1; then
    printf "✓ %s\n" "$name"; pass=$((pass+1))
  else
    printf "✗ %s\n" "$name"; fail=$((fail+1))
  fi
}

# ===== Agent tooling (C-2, C-3) =====
check "doc-scanner has Write tool" \
  "grep -Eq '^\s*-\s*Write\b' agents/doc-scanner.md"

check "command uses Task (not Agent) in allowed-tools" \
  "grep -Eq '^allowed-tools:.*\bTask\b' commands/deep-docs.md"

check "command does NOT use Agent in allowed-tools" \
  "! grep -Eq '^allowed-tools:.*\bAgent\b' commands/deep-docs.md"

check "command body uses Task(subagent_type=...) pseudo-code" \
  "grep -q 'Task(subagent_type=' commands/deep-docs.md"

check "no Agent(doc-scanner): pseudo-code remains" \
  "! grep -q 'Agent(doc-scanner):' commands/deep-docs.md"

# ===== Scan-filters integration =====
check "scan-rules references scan-filters directory" \
  "grep -q 'scan-filters/' skills/deep-docs-workflow/references/scan-rules.md"

check "doc-scanner references scan-filters in Step 2/6" \
  "grep -c 'scan-filters/' agents/doc-scanner.md | awk '{exit \$1 < 2}'"

for filter in translation-pair code-fence reference-extraction cli-whitelist worktree-hash freshness-timestamp; do
    check "scan-filters/${filter}.md exists" \
      "[ -f skills/deep-docs-workflow/references/scan-filters/${filter}.md ]"
done

# ===== Artifact provenance (H-1, H-2, H-5) =====
check "schema_version == 2 in doc-scanner example" \
  "grep -Eq '\"schema_version\":\s*2' agents/doc-scanner.md"

check "worktree_hash in doc-scanner" \
  "grep -q 'worktree_hash' agents/doc-scanner.md"

check "worktree_hash in commands" \
  "grep -q 'worktree_hash' commands/deep-docs.md"

check "worktree_hash in SKILL.md" \
  "grep -q 'worktree_hash' skills/deep-docs-workflow/SKILL.md"

check "4-factor reuse rule documented" \
  "grep -Eq '4-요소|4-factor|schema_version.*일치|schema_version ==' commands/deep-docs.md skills/deep-docs-workflow/SKILL.md"

# ===== Freshness & audit (H-4, M-1, M-2, M-3) =====
check "freshness_score example uses valid scale (not 6)" \
  "! grep -q '\"freshness_score\":\s*6' agents/doc-scanner.md"

check "stale ratio thresholds 30%/70% documented" \
  "grep -Eq '30%.*70%|<30%|≥70%' skills/deep-docs-workflow/references/audit-metrics.md"

check "band uses strict inequality (≥ 9.0)" \
  "grep -Eq 'score ≥ 9\.0|≥ 9\.0' README.md README.ko.md skills/deep-docs-workflow/references/audit-metrics.md"

check "size warnings use strict > (not ≥)" \
  "grep -Eq '>100|>300|>200' skills/deep-docs-workflow/references/scan-rules.md skills/deep-docs-workflow/references/audit-metrics.md"

# ===== Issue field rename (M-6) =====
check "current_value / suggested_value documented" \
  "grep -Eq 'current_value.*suggested_value|suggested_value.*current_value' agents/doc-scanner.md"

check "old 'reference'/'suggestion' fields not in current example" \
  "! grep -Eq '\"reference\":\s*\"src/' agents/doc-scanner.md"

# ===== Garden UX (M-8, M-9) =====
check "garden-ignored.json documented" \
  "grep -q 'garden-ignored.json' commands/deep-docs.md"

check "garden 5지선다 options A-E present" \
  "grep -c '(A) 적용\\|(B) 건너뜀\\|(C) 건너뜀.*기록\\|(D) 이하 모두 적용\\|(E) 이하 모두 건너뜀' commands/deep-docs.md | awk '{exit \$1 < 3}'"

# ===== Housekeeping (L-1~L-5) =====
check "package.json has private: true" \
  "grep -q '\"private\":\s*true' package.json"

check "hooks/hooks.json removed" \
  "[ ! -f hooks/hooks.json ]"

check ".gitignore includes .deep-docs/" \
  "grep -q '^\.deep-docs/' .gitignore"

check "docs/backlog-2026-04-16.md committed" \
  "git ls-files --error-unmatch docs/backlog-2026-04-16.md > /dev/null 2>&1"

# ===== Platform compat (O-6) =====
check "scan-filters use shasum -a 1 (not sha1sum)" \
  "grep -q 'shasum -a 1' skills/deep-docs-workflow/references/scan-filters/worktree-hash.md"

check "worktree-hash.md does NOT use xargs -I{} sh -c" \
  "! grep -Eq \"xargs -I\{\}.*sh -c\" skills/deep-docs-workflow/references/scan-filters/worktree-hash.md"

# ===== Version sync =====
check "plugin.json version = 1.1.0" \
  "grep -q '\"version\":\s*\"1.1.0\"' .claude-plugin/plugin.json"

check "package.json version = 1.1.0" \
  "grep -q '\"version\":\s*\"1.1.0\"' package.json"

check "CHANGELOG has [1.1.0] entry" \
  "grep -q '\\[1.1.0\\]' CHANGELOG.md"

# ===== Result =====
echo "---"
echo "Passed: $pass  Failed: $fail"
[ "$fail" -eq 0 ] || exit 1
```

- [ ] **Step 5: verify-fixes.sh 실행 가능 권한**

```bash
chmod +x scripts/verify-fixes.sh
```

- [ ] **Step 6: verify-fixes.sh 실행**

```bash
cd /Users/sungmin/Dev/deep-docs
bash scripts/verify-fixes.sh
```

Expected: 모든 체크 `✓` 통과 (30+ 체크), 마지막 줄 `Passed: N  Failed: 0`.

**실패 시**: 어느 체크가 실패했는지 확인하고, 이전 Task에서 누락된 변경을 찾아 수정 후 재실행. 모두 통과할 때까지 commit 하지 않음.

- [ ] **Step 7: Dogfood scan 실행**

```bash
# Claude Code 세션에서:
/deep-docs scan
```

**성공 기준** (스펙 §7.5):
1. `README.md ↔ README.ko.md` JSON 중복이 **audit-only** 분류 (번역 쌍 필터 동작)
2. README 내 `src/auth/middleware.ts` 예시가 dead-reference 후보에 올라오지 **않음** (코드펜스 내부)
3. `.deep-docs/last-scan.json` 파일이 생성되고 `schema_version: 2`, `provenance.worktree_hash` 필드 존재
4. `/deep-docs audit` 실행 시 소수점 1자리 점수 (예: `8.5/10`)
5. 새 untracked 파일 추가 후(`touch /tmp/test-untracked.md && mv /tmp/test-untracked.md docs/`) 재-scan 시 `worktree_hash` 값 변경

각 기준을 수동 확인 후 PR description에 체크박스로 기록.

- [ ] **Step 8: Commit**

```bash
git add .claude-plugin/plugin.json package.json CHANGELOG.md scripts/verify-fixes.sh
git commit -m "chore(release): v1.1.0 — bump version, changelog, verify script

- plugin.json and package.json 1.0.0 → 1.1.0
- CHANGELOG.md [1.1.0] section — Breaking/Added/Changed/Removed/Fixed/Security
- scripts/verify-fixes.sh — 30+ grep checks for spec compliance

All checks passing via: bash scripts/verify-fixes.sh
Dogfood /deep-docs scan verified: translation-pair, worktree_hash,
freshness scale, size thresholds all functioning as specified."
```

---

## Final Verification

모든 Task 완료 후:

```bash
cd /Users/sungmin/Dev/deep-docs

# 1. Verify script
bash scripts/verify-fixes.sh

# 2. git 상태
git log --oneline -10
git status

# 3. 실제 플러그인 동작 확인
/deep-docs scan    # Claude Code 세션에서
/deep-docs audit
```

모두 통과하면 PR 생성 준비 완료.

---

## Rollback Strategy

각 Task는 독립 커밋이므로 `git revert <sha>`로 개별 롤백 가능. **revert 순서는 역순 권장** (Task 8 → 7 → ... → 1): 동일 파일을 여러 Task가 수정하는 경우(특히 `agents/doc-scanner.md`, `README` 양종)가 있어 순방향 revert는 충돌 발생 가능. 충돌 시 수동 해결.

자세한 파일 중첩 매트릭스는 spec §8 참조.

---

## PR 생성

모든 커밋 완료 후:

```bash
git push -u origin main   # 또는 feature branch

gh pr create --title "Deep Docs v1.1.0 — 24 findings from ultrareview" --body "$(cat <<'EOF'
## Summary

Implements spec `docs/superpowers/specs/2026-04-17-deep-docs-ultrareview-fixes-design.md` (v6, 6 라운드 deep-review APPROVE).

24 findings from `docs/ultrareview-2026-04-17.md` addressed across 8 logical commits. Scan heuristics split into `scan-filters/` directory with Python primary implementation and Bash reference.

## Test Plan

- [ ] `bash scripts/verify-fixes.sh` — 모든 체크 통과
- [ ] `/deep-docs scan` on deep-docs repo itself (dogfood)
- [ ] README 번역 쌍 JSON 중복이 audit-only로 분류됨
- [ ] 코드펜스 내 예시 경로가 dead-reference 후보에 들지 않음
- [ ] `last-scan.json`에 `schema_version: 2`, `worktree_hash` 필드 존재
- [ ] Audit 점수 소수점 1자리 표시
EOF
)"
```

---

## Self-Review Check

**1. Spec 커버리지:** 24 findings + 7 new (round 2 N-series + BU-1~BU-8 + round 5/6) → 8 Task에 매핑:
- Task 1: C-2, C-3, M-4
- Task 2: C-1, H-3, M-7, X-1, X-3, X-4, NC-1, NC-2, NEW-RCE, NEW-CLI-BYPASS, NEW-FENCE-INDENT, CX-1, BU-1~BU-8, N-1, N-2, N-5, N-6, W-1
- Task 3: H-1, H-2, H-5, O-4
- Task 4: H-4, H-6, M-1, BU-5, BU-6, N-5
- Task 5: M-2, M-3, L-6, CX-2, O-10
- Task 6: M-5, M-6, O-3
- Task 7: M-8, M-9, L-1~L-5, O-7
- Task 8: 버전, CHANGELOG, verify, dogfood

**2. Placeholder scan:** 코드 블록 전부 실행 가능, TBD 없음.

**3. Type consistency:** `worktree_hash` (스펙/필터), `schema_version: 2` (아티팩트), `current_value`/`suggested_value` (issue) — 모든 Task에서 일관.

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-04-17-deep-docs-v1.1.0-fixes.md`.**

두 가지 실행 옵션:

1. **Subagent-Driven (권장)** — 각 Task마다 fresh subagent 파견, Task 간 리뷰, 빠른 iteration. 이 plan의 8개 Task가 독립적이라 적합.

2. **Inline Execution** — 현 세션에서 executing-plans 스킬로 batch 실행, 중간 체크포인트.

**어느 방식으로 진행할까요?**
